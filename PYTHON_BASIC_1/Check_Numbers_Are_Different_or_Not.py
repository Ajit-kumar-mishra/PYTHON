def seq_of_num(data):
    for values in data:
        for x in data:
            if x==values:
                print("all numbers are not different")
            else:
                print("all numbers are distinct")
                
                
            